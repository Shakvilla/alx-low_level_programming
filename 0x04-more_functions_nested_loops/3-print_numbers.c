#include "main.h"
/**
 * print_numbers - prints numbers from 0 - 9
 *
 * Return: no return
 */
void print_numbers(void)
{
	char ch;

	for (ch < 8; ch <= 57; ch++)
	{
		_putchar(ch)
	}
	_putchar('\n');

}
